# Smart VMS — example environment (copy to .env, never commit .env)
# Phase 1+ implementation

# Web UI Copilot (see web/.env.example)
# VITE_OLLAMA_MODEL=qwen2.5-coder:7b

# Axis camera (per-camera config may move to DB later)
# AXIS_CAMERA_HOST=192.168.1.50
# AXIS_VAPIX_USER=smartvms_svc
# AXIS_VAPIX_PASSWORD=

# Server
# DATABASE_URL=postgresql://smartvms:changeme@localhost:5432/smartvms
# MQTT_BROKER_URL=mqtt://localhost:1883
# S3_ENDPOINT=http://localhost:9000
# S3_BUCKET=smart-vms-clips
